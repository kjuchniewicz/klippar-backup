#### crowsnest.conf
#### This is the default config after installation.
#### It is also used as the default config in MainsailOS.
#### For details on how to configure this to your needs, see:
#### https://github.com/mainsail-crew/crowsnest/blob/v5/README.md


#####################################################################
####                                                            #####
####      Information about ports and according URL's           #####
####                                                            #####
#####################################################################
####                                                            #####
####    Port 8080 equals /webcam/?action=[stream/snapshot]      #####
####    Port 8081 equals /webcam2/?action=[stream/snapshot]     #####
####    Port 8082 equals /webcam3/?action=[stream/snapshot]     #####
####    Port 8083 equals /webcam4/?action=[stream/snapshot]     #####
####                                                            #####
####    Note: These ports are default for most Mainsail         #####
####    installations. Using any other port would involve       #####
####    changing the proxy configuration or using URLs          #####
####    with the specific port like                             #####
####    http://<ip>:<port>/?action=[stream/snapshot]            #####
####                                                            #####
#####################################################################


[crowsnest]
log_level: verbose                      # Valid Options are quiet/verbose/debug
rollover_on_start: false                # Creates a backup and clears the log on every restart, if set to true
no_proxy: true                         # If set to true, no reverse proxy is required. Only change this, if you know what you are doing.

[cam PrintCam]
mode: ustreamer                         # https://docs.mainsail.xyz/crowsnest/faq/backends
port: 8080                              # HTTP/MJPG stream/snapshot port
device: /dev/v4l/by-id/usb-046d_0825_66207860-video-index0                    # See log for available devices
resolution: 1280x720                    # <width>x<height> format
max_fps: 10                             # If hardware supports it, it will be forced, otherwise ignored/coerced.
# custom_flags: --format=yuyv                         # You can run the stream services with custom flags.
v4l2ctl:                               # Add v4l2-ctl parameters to set up your camera, see log for your camera capabilities.
	brightness=128,
    contrast=41,
    saturation=45,
    white_balance_automatic=0,
    gain=55,
    power_line_frequency=1,
    white_balance_temperature=4300,
    sharpness=24,
    backlight_compensation=0,
    auto_exposure=1,
    exposure_time_absolute=700,
    exposure_dynamic_framerate=0

[cam MainCam]
mode: ustreamer                         # https://docs.mainsail.xyz/crowsnest/faq/backends
port: 8081                              # HTTP/MJPG stream/snapshot port
device: /dev/v4l/by-id/usb-046d_0825_B425FE40-video-index0                     # See log for available devices
resolution: 1280x720                    # <width>x<height> format
max_fps: 10                             # If hardware supports it, it will be forced, otherwise ignored/coerced.
# custom_flags: --format=yuyv                          # You can run the stream services with custom flags.
v4l2ctl:                               # Add v4l2-ctl parameters to set up your camera, see log for your camera capabilities.
	brightness=128,
    contrast=41,
    saturation=45,
    white_balance_automatic=0,
    gain=75,
    power_line_frequency=1,
    white_balance_temperature=4150,
    sharpness=24,
    backlight_compensation=0,
    auto_exposure=1,
    exposure_time_absolute=750,
    exposure_dynamic_framerate=0

[cam RoomCam]
mode: ustreamer                         # https://docs.mainsail.xyz/crowsnest/faq/backends
port: 8082                              # HTTP/MJPG stream/snapshot port
device: /dev/v4l/by-id/usb-Creative_Technology_Ltd._Live__Cam_Sync_HD_VF0770-video-index0                     # See log for available devices
resolution: 1280x720                    # <width>x<height> format
max_fps: 10                             # If hardware supports it, it will be forced, otherwise ignored/coerced.
custom_flags: --format=yuyv                          # You can run the stream services with custom flags.
v4l2ctl:                               # Add v4l2-ctl parameters to set up your camera, see log for your camera capabilities.
	brightness=10,
    contrast=5,
    saturation=45,
    hue=0,
    white_balance_automatic=1,
    gamma=115,
    power_line_frequency=1,
    # white_balance_temperature=4300,
    sharpness=2,
    backlight_compensation=0,
    auto_exposure=3
    # exposure_time_absolute=60